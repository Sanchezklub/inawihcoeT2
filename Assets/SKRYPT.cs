using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class NewBehaviourScript : MonoBehaviour
{
    bool Rozbity=false;
    bool Otwarte=false;
    void Start()
    {
            If (Rozbity==false && Gość jest w rejonie && atakuje )
        {

            Rozbite=True;
        }
            If ( Otwarte==false && gość jest w rejonie && klikniety e )
            {
                Pojawienie przedmiotu
                zmiana zdjecia
                Otwarte=true;
            }
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
